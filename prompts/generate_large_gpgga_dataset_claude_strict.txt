You are generating a production-like synthetic dataset for a UAV terrain-navigation project.

Your task is to output complete file contents, not explanations.
Do not summarize.
Do not shorten.
Do not replace content with placeholders.
Do not say "the rest is similar".
Do not invent a different folder structure.

You must generate files that fit exactly into this project layout:

input/incoming/config.yaml
input/incoming/radar_data.nmea
input/incoming/truth.csv
input/incoming/barometer.csv
input/incoming/dem/README.txt

Critical constraints:

1. The primary input stream must be only NMEA-0183 `$GPGGA` sentences.
2. Every line in `radar_data.nmea` must be a valid `$GPGGA` sentence.
3. Every `$GPGGA` line must have a correct checksum.
4. `radar_data.nmea` and `truth.csv` must describe the same flight.
5. Latitude/longitude in NMEA must be mathematically consistent with `truth.csv`.
6. Output must be directly savable into files without code changes.
7. If the answer is too large, split by file and by numbered parts.
8. The barometric altitude must represent an absolute flight altitude of about 1500 meters above mean sea level.
9. The NMEA message rate must be within 1 to 10 Hz.
10. The dataset must be suitable for terrain navigation over a real DEM from Copernicus GLO-30, SRTM, or ALOS AW3D30.

Generate the following files:

----------------------------------------------------------------
FILE 1: input/incoming/radar_data.nmea
----------------------------------------------------------------

Requirements:
- Plain text file.
- Minimum 50,000 lines.
- Each line is exactly one valid `$GPGGA` sentence.
- Use only the `GPGGA` message type.
- Message rate must be within 1 to 10 Hz. Use a consistent rate and reflect it in `config.yaml`.
- UTC time must advance sequentially.
- Latitude and longitude must be populated and realistic.
- Fix quality must be valid.
- Number of satellites must be realistic.
- HDOP must be realistic.
- Altitude field must be populated.
- Altitude units must be `M`.
- Geoid separation can be realistic and fixed or slowly varying.
- Checksum must be correct for every line.
- The altitude field must evolve smoothly and realistically.
- The motion must resemble a UAV flight suitable for terrain navigation evaluation.

Use this line shape:
$GPGGA,123519.111,5545.1234,N,03736.5678,E,1,08,0.9,145.6,M,46.9,M,,*47

Do not output malformed NMEA.
Do not output missing checksum.
Do not output mixed talker messages.

----------------------------------------------------------------
FILE 2: input/incoming/truth.csv
----------------------------------------------------------------

Output strict CSV with this exact header:
timestamp,lat,lon,alt_msl,heading_deg,speed_mps

Requirements:
- Minimum 50,000 rows after the header.
- `timestamp` must be monotonically increasing.
- `lat` and `lon` must match the same flight represented in `radar_data.nmea`.
- `alt_msl` must evolve smoothly.
- `heading_deg` must be consistent with the trajectory.
- `speed_mps` must be realistic for a UAV.
- Include straight segments, smooth turns, climb, descent, and speed changes.
- Use decimal degrees for `lat` and `lon`.
- Use seconds with fractional part for `timestamp`.

----------------------------------------------------------------
FILE 3: input/incoming/barometer.csv
----------------------------------------------------------------

Output strict CSV with this exact header:
timestamp,baro_alt_m

Requirements:
- Minimum 50,000 rows after the header.
- `timestamp` must align with `truth.csv`.
- `baro_alt_m` must stay close to 1500 m MSL and remain consistent with `truth.csv`.
- Add realistic noise.
- Add a small smooth drift over time.
- Do not use purely random uncorrelated noise.

----------------------------------------------------------------
FILE 4: input/incoming/config.yaml
----------------------------------------------------------------

Output exactly one YAML file with these fields and values:

dem_path: input/incoming/dem/terrain.tif
radar_data_path: input/incoming/radar_data.nmea
truth_path: input/incoming/truth.csv
barometer_path: input/incoming/barometer.csv
sample_rate_hz: 5.0

correlation:
  profile_length_m: 5000.0
  step_m: 30.0
  max_offset_m: 2000.0
  flat_terrain_threshold_m: 15.0
  cold_start_windows: 3

visualization:
  enabled: true
  dashboard_host: 127.0.0.1
  dashboard_port: 8050
  export_report_path: output/terrain_navigator_report.html

Do not rename keys.
Do not add commentary outside the YAML.

----------------------------------------------------------------
FILE 5: input/incoming/dem/README.txt
----------------------------------------------------------------

Output a short plain-text note saying that a DEM file in GeoTIFF or SRTM format must be placed in this folder, for example `terrain.tif`.

----------------------------------------------------------------
Global consistency requirements
----------------------------------------------------------------

- The whole dataset must be internally consistent.
- `radar_data.nmea`, `truth.csv`, and `barometer.csv` must describe the same timeline.
- The generated data must look realistic for terrain navigation testing.
- `truth.csv` should keep absolute altitude near 1500 m MSL unless a specific mild climb/descent segment is intentionally introduced.
- Do not generate toy data.
- Do not generate obviously random nonsense.
- Do not omit lines because of answer length; split into parts instead.

----------------------------------------------------------------
Required output format
----------------------------------------------------------------

Output file contents only.
Before each file or file part, print a marker in this exact style:

=== input/incoming/radar_data.nmea part 1/N ===
=== input/incoming/truth.csv part 1/N ===
=== input/incoming/barometer.csv part 1/N ===
=== input/incoming/config.yaml ===
=== input/incoming/dem/README.txt ===

No markdown code fences unless absolutely necessary.
No analysis.
No notes.
No explanations.
Just the file contents in the specified order.
