Place your DEM file here.

Supported input examples:
- GeoTIFF (*.tif, *.tiff)
- SRTM (*.hgt)

Recommended default filename for quick start:
- terrain.tif

If you use a different filename, update `dem_path` in `input/incoming/config.yaml`.
